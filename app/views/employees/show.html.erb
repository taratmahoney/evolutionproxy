
  <div class="employee-title">

    <h2><%= @employee.first_name %> <%= @employee.last_name %></h2>
    <h3><%= @employee.title %></h3>

  </div>
<div class="container-medium">  
    <div class="fr-view">
      <%= raw sanitize(simple_format(@employee.description), :tags => %w(br p) ) %>
    </div>

    <div class="toolbar"> 
  <%= link_to edit_employee_path do %> <i class="fa fa-pencil-square-o"></i> <% end %> | <%= link_to  @employee, method: :delete, data: {confirm: "Are you sure you want to delete this?"} do %> <i class="fa fa-trash-o"></i> <% end %>
  </div>

</div>